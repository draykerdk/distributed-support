title: "Distributed support"
description: "Common capacity that helps members, projects, communities and infrastructure become able to continue and grow — the human, project and network substrate of Drayker, related through the economy of capacity."
remote_theme: draykerdk/drayker-theme
url: "https://support.drayker.org"
dk_kicker: "Support layer"
dk_subdomain: "support.drayker.org"
dk_repo: "https://github.com/draykerdk/distributed-support"


# jekyll-remote-theme fetches the shared layout; jekyll-optional-front-matter lets
# README.md render without front matter; the defaults block is what actually applies
# the layout to it. Without all three, GitHub Pages serves bare converted markdown.
plugins:
  - jekyll-remote-theme
  - jekyll-optional-front-matter
  - jekyll-sitemap

defaults:
  - scope:
      path: ""
    values:
      layout: default

# The official mark, recoloured. Geometry is fixed for every project
# (DRAYKER-MARK.md §2.5); only the globe carries the scope.
dk_accent: "#8FA8C4"  # the machines: steel holding the thing up
